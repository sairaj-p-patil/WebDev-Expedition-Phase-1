const sum = (a, b) => { // implicit of his writte down
    console.log(a+b);
};

sum(2,3);
// console
// 5

//const cube = (n) =>
{
    return n * n * n;
};

const pow = (a, b) => {
    return a**b;
};

//------------------------------------------------------------
 // IMPLICIT  ARROW FUUNCTION //


 const mul = (a, b) => a * b; // mul(2,3)=6 same with up all 
 mul(2,3);   

 const sum = (a, b) => a + b; //sum(2,3)=5

 const cube = (n) => n * n * n; //cube(2,3)=8

